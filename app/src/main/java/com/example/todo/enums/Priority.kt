package com.example.todo.enums

/**
 * Enum representing the priority levels for tasks
 * Used to categorize tasks based on their level of urgency or importance
 */
enum class Priority {
    LOW, MEDIUM, HIGH
}
